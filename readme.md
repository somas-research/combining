# Repository for combining MLSM with sparse contextualized word representations

For usage install `requirements.txt`, download the WSD_Evaluation_Framework and the wsd_hard_benchmark and run `main.py`.

#TODO config description